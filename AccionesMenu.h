#include "Lista.h"
#include "Validar.h"

class AccionesMenu
{
private:
public:
    void ingresarTurno(Lista*);
    void eliminarTurno(Lista*);
    void imprimirTurno(Lista*);
    AccionesMenu();
    ~AccionesMenu();
};

